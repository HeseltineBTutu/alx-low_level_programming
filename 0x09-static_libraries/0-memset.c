#include "main.h"
/**
 * _memset- entry point
 *
 * @s:a pointer to the memory block that you want to fill withe given value.
 *
 * @b: The value that you want to fill the memory block with.
 *
 * @n: The number of bytes that you want to fill with the given value.
 *
 * Return: a pointer to the memory block that was filled with the given value.
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
